<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- The browser supports explicit http:// navigation, matching iOS WebView. -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>
</network-security-config>
