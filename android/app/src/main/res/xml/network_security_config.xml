<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- The browser supports explicit http:// navigation, matching iOS WebView. -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>

    <!-- Keep configured DNS-over-HTTPS providers HTTPS-only. -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">cloudflare-dns.com</domain>
        <domain includeSubdomains="true">dns.google</domain>
        <domain includeSubdomains="true">dns.quad9.net</domain>
        <domain includeSubdomains="true">dns.nextdns.io</domain>
        <domain includeSubdomains="true">dns.adguard.com</domain>
    </domain-config>
</network-security-config>
